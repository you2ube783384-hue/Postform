[
  {
    "name": "testsprite_bootstrap",
    "description": "BEFORE calling this tool, check if testsprite_tests/tmp/config.json exists in the project. If config.json EXISTS, DO NOT call this tool - proceed directly with other tools instead. This tool is ONLY for first-time project initialization when no configuration exists. NEVER call for: adding new test cases, modifying tests, re-running tests, or any operation on an already-configured project. Note: this MCP tests a locally-running app — it tunnels your local server, so make sure it's running before you proceed.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "type": "object",
      "properties": {
        "localPort": {
          "description": "The localPort of the local service is usually defined in the codebase, such as in the Dockerfile. \n       Alternatively, you can determine it based on the language and framework used in the project.",
          "default": 5173,
          "type": "number",
          "minimum": 1,
          "maximum": 65535
        },
        "pathname": {
          "description": "Webpage path (without domain). Defaults to an empty string if not provided.",
          "type": "string"
        },
        "type": {
          "description": "The type of test, can be detected from the codebase",
          "type": "string",
          "enum": [
            "frontend",
            "backend"
          ]
        },
        "projectPath": {
          "description": "Absolute path of current project root",
          "type": "string"
        },
        "testScope": {
          "description": "The scope of the test:\n  - codebase: entire codebase;\n  - diff: staged changes that has not been commited;",
          "type": "string",
          "enum": [
            "codebase",
            "diff"
          ]
        }
      },
      "required": [
        "type",
        "projectPath",
        "testScope"
      ],
      "additionalProperties": false
    }
  },
  {
    "name": "testsprite_generate_code_summary",
    "description": "Analyze the project repository and summarize the codebase.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "type": "object",
      "properties": {
        "projectRootPath": {
          "description": "Absolute path of current project root",
          "type": "string"
        }
      },
      "required": [
        "projectRootPath"
      ],
      "additionalProperties": false
    }
  },
  {
    "name": "testsprite_generate_standardized_prd",
    "description": "Generate a structured standard PRD.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "type": "object",
      "properties": {
        "projectPath": {
          "description": "Absolute path of current project root",
          "type": "string"
        }
      },
      "required": [
        "projectPath"
      ],
      "additionalProperties": false
    }
  },
  {
    "name": "testsprite_generate_frontend_test_plan",
    "description": "Generate test plan for frontend test.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "type": "object",
      "properties": {
        "projectPath": {
          "description": "Absolute path of current project root",
          "type": "string"
        },
        "needLogin": {
          "default": true,
          "description": "Whether to include login in the test plan, only for frontend test",
          "type": "boolean"
        }
      },
      "required": [
        "projectPath"
      ],
      "additionalProperties": false
    }
  },
  {
    "name": "testsprite_generate_backend_test_plan",
    "description": "Generate a backend test plan with Testsprite.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "type": "object",
      "properties": {
        "projectPath": {
          "description": "Absolute path of current project root",
          "type": "string"
        }
      },
      "required": [
        "projectPath"
      ],
      "additionalProperties": false
    }
  },
  {
    "name": "testsprite_generate_code_and_execute",
    "description": "Generate and execute tests. Before calling this tool, ensure the local project is running — preferably in production mode (e.g. `npm run build && npm run start` for Next.js, `npm run build && npm run preview` for Vite). If you started the project with a production build+serve command, set serverMode to 'production' (note: serverMode is also auto-detected from the running process). If running a dev server (e.g. npm run dev), leave serverMode as default — frontend tests will be limited to 15 high-priority tests to prevent dev server overload. If local server is running and test plan is already generated, call this tool directly — DO NOT call testsprite_bootstrap first. AI assistant analyzes the test results and saves a markdown report. This runs against your locally-running server.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "type": "object",
      "properties": {
        "projectName": {
          "description": "The name is the name of the root directory of the current project",
          "type": "string"
        },
        "projectPath": {
          "description": "Absolute path of current project root",
          "type": "string"
        },
        "testIds": {
          "default": [],
          "description": "The test ids of the test plan, default is empty array, means all test plan will be applied",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "additionalInstruction": {
          "default": "",
          "description": "Additional instructions for test generation and execution, set to empty string if not specified by the user",
          "type": "string"
        },
        "serverMode": {
          "description": "Whether the local server is running in production or development mode. AI should detect this from how the server was started (build+start = production, dev = development). Defaults to development.",
          "default": "development",
          "type": "string",
          "enum": [
            "production",
            "development"
          ]
        }
      },
      "required": [
        "projectName",
        "projectPath"
      ],
      "additionalProperties": false
    }
  },
  {
    "name": "testsprite_open_test_result_dashboard",
    "description": "Open an interactive web dashboard to review and modify finished test cases. Use this tool when users want to review test results, fix failing tests, or modify test steps. The dashboard displays all test cases with their execution status, allows users to view test details, edit individual steps, and re-run tests from any step. Prerequisites: (1) Tests must have been generated first using 'testsprite_generate_code_and_execute', (2) Local project must be running on the configured port for re-running tests.",
    "inputSchema": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "type": "object",
      "properties": {
        "projectPath": {
          "description": "Absolute path of current project root",
          "type": "string"
        },
        "modificationContext": {
          "description": "Context about the test results or modifications to review",
          "type": "string"
        }
      },
      "required": [
        "projectPath"
      ],
      "additionalProperties": false
    }
  },
  {
    "name": "testsprite_check_account_info",
    "description": "Check the current user's TestSprite account information including name, subscription plan, credits, and email address.",
    "inputSchema": {
      "type": "object",
      "properties": {},
      "additionalProperties": false
    }
  }
]
